'use strict';

var _ = require('lodash');
var colors = require('colors');

var Composer = function(inputs, generator) {
  this.inputs = inputs;
  this.generator = generator;
};

Composer.prototype.compose = function () {

};

modules.exports = Composer;
